'use client';

import React from 'react';
import { GlobalStyle as StyledGlobalStyle } from '../../styles/global';

const GlobalStyle = () => {
  return <StyledGlobalStyle />;
};

export default GlobalStyle;
